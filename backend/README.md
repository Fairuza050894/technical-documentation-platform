# Backend

FastAPI application using a `src` layout and Clean Architecture module boundaries.

```bash
uv sync --project backend --all-groups
uv run --project backend uvicorn tdp.main:app --reload --port 8000
```
